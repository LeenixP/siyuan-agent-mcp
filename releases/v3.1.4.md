# v3.1.4

## Fixes

- Fixed `siyuan_move_block` parentID-only moves, including moving a block under a document ID as the first child.
- Omitted absent `moveBlock` anchor fields instead of sending empty strings, avoiding SiYuan kernel validation of an empty `previousID`.

## Tests

- Added regression coverage for `siyuan_move_block` parentID-only and previousID-only request bodies.

## Validation

- `npm test`
